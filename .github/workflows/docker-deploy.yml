name: Docker Build and Deploy

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  DOCKER_IMAGE: ${{ secrets.DOCKERHUB_USERNAME }}/portfolio-dev
  
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ env.DOCKER_IMAGE }}:latest
            ${{ env.DOCKER_IMAGE }}:${{ github.sha }}
          cache-from: type=registry,ref=${{ env.DOCKER_IMAGE }}:buildcache
          cache-to: type=registry,ref=${{ env.DOCKER_IMAGE }}:buildcache,mode=max

#   deploy-to-coolify:
#     needs: build-and-push
#     runs-on: ubuntu-latest
#     steps:
#       - name: Deploy to Coolify
#         uses: usebottles/coolify-deployment-action@v1
#         with:
#           deployment_url: ${{ secrets.COOLIFY_URL }}
#           token: ${{ secrets.COOLIFY_TOKEN }}
#           resource_id: "your-resource-id" # Reemplaza con tu ID de recurso de Coolify
#           force_deployment: true
          
#       - name: Deployment Status
#         run: |
#           echo "Deployment to Coolify completed"
#           echo "Image: ${{ env.DOCKER_IMAGE }}:${{ github.sha }}" 